import React, { Component } from 'react';

class Fileuploader extends Component {
  state = {
    name: ''
  };

  render(){
    return (
      <div>
        Uploader
      </div>
    );
  }
}

export default Fileuploader;